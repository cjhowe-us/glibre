cmake_minimum_required(VERSION 3.30)

# ---------------------------------------------------------------------------
# tests/ — Catch2 unit test suite
#
# Each bounded context owns a subdirectory here.  Add new contexts as
# their first unit tests land.
# ---------------------------------------------------------------------------

add_subdirectory(data)
