#!/usr/bin/env bash
# Reject PRs whose linked task issue has unresolved blockers.
# Reads `Closes #N` / `Refs #N` from PR body, checks issue dependencies via gh.
# TODO: implement once GitHub repo + first issues exist.
set -euo pipefail
echo "dep-check: not yet implemented; passing"
