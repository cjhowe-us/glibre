cmake_minimum_required(VERSION 3.30)

# ---------------------------------------------------------------------------
# examples/ — reference plugins and example code
#
# These targets are compile / link tests and canonical scaffolds for plugin
# authors.  They are NOT shipped in distribution builds.
#
# Add new example subdirectories here as they land.
# ---------------------------------------------------------------------------

add_subdirectory(plugin-noop)
